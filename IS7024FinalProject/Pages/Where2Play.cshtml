@page
@model IS7024FinalProject.Pages.Where2PlayModel

@{
    ViewData["Title"] = "Concert Event Search";
}

<div class="container my-5">
    <div class="text-center mb-4">
        <h1 class="display-4 text">Where2Play Concert Finder</h1>
        <p class="lead text-muted">Searching for events in **@Model.City** powered by the Where2Play API.</p>
    </div>

    <!-- City Search Form -->
    <div class="row justify-content-center mb-5">
        <div class="col-12 col-md-8 col-lg-6">
            <form method="get" class="d-flex shadow-lg rounded-pill p-1 bg-white border border-primary">
                <input class="form-control rounded-pill me-2 border-0" type="search" name="City"
                    placeholder="Enter a city (e.g., Nashville)" value="@Model.City" aria-label="Search by City"
                    required />
                <button class="btn btn-primary rounded-pill px-4 text-white fw-bold" type="submit">Search Events</button>
            </form>
        </div>
    </div>

    <!-- Display Error Message if any -->
    @if (!string.IsNullOrEmpty(Model.ErrorMessage))
    {
        <div class="alert alert-danger text-center shadow" role="alert">
            <h4 class="alert-heading">Request Failed!</h4>
            <p>@Model.ErrorMessage</p>
        </div>
    }

    <!-- Display Results -->
    @if (Model.Events.Count > 0)
    {
        <h2 class="h4 mb-4 text-center text-secondary">Found @Model.Events.Count Concerts in @Model.City</h2>

        <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
            @foreach (var eventItem in Model.Events)
            {
                <div class="col">
                    <div class="card h-100">
                        <!-- Artist Name -->
                            <div class="card-header text-center">
                                <h5>
                                    @eventItem.ArtistName
                                </h5>
                            </div>
                        <div class="card-body text-center d-flex flex-column justify-content-between">




                            <!-- Genre -->
                            @if (!string.IsNullOrEmpty(eventItem.Genre))
                            {
                                <span class="badge bg-secondary mb-3">@eventItem.Genre</span>
                            }

                            <!-- Event Details -->
                            <div class="mt-2">
                                <p class="card-text mb-1 mt-3">
                                    <span class="event-detail-label d-block text-uppercase text-start">Venue:</span>
                                    <span class="fw-bold text-dark fs-6">@eventItem.Venue</span>
                                </p>
                                <p class="card-text mb-1 mt-3">
                                    <span class="event-detail-label d-block text-uppercase text-start">When:</span>
                                    <span class="fw-bold text-dark fs-6">@eventItem.FormattedDate</span>
                                </p>
                                @if (!string.IsNullOrEmpty(eventItem.Country))
                                {
                                    <p class="card-text mb-1 mt-3">
                                        <span class="event-detail-label d-block text-uppercase text-start">Origin:</span>
                                        <span class="fw-bold text-dark fs-6">@eventItem.Country</span>
                                    </p>
                                }
                                <p class="card-text mb-1 mt-3">
                                    <span class="event-detail-label d-block text-uppercase text-start">Popularity:</span>
                                    <span class="fw-bold text-dark fs-6">@eventItem.Popularity</span>
                                </p>
                            </div>

                            <!-- Link to details -->
                            <div class="card-footer text-center border-0 p-3">
                                @if (!string.IsNullOrEmpty(eventItem.Url))
                                {
                                    <a href="@eventItem.Url" target="_blank" class="btn btn-outline-light btn-sm">
                                        View Setlist/Details &rarr;
                                    </a>
                                }
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
    else if (Model.Events.Count == 0 && !string.IsNullOrWhiteSpace(Model.City))
    {
        <!-- Display if search was run but returned no results -->
        <div class="alert alert-light text-center border shadow-sm" role="alert">
            <h4 class="alert-heading text-secondary">No Concerts Found!</h4>
            <p>There are no concert events listed for **@Model.City** at this time, or the city is not one of the supported
                demo cities.</p>
        </div>
    }
</div>